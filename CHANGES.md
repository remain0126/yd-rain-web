# yd-rain-web 수정분 (2026-09-06)

저장소 같은 경로에 그대로 덮어쓰면 됩니다. 아래 18개 외의 파일은 손대지 않았습니다.

## 파일 목록

| 파일 | 내용 |
|---|---|
| `netlify/functions/_history.js` | 창 기준을 시계로. 표 정체 시간 계산. 빈칸 0 확정 저장 |
| `netlify/functions/_build.js` | `table_lag_h`, `date_mismatch`, 결측 칸 수를 응답에 실음 |
| `netlify/functions/_push.js` | 구독을 사람별 키로 분리. 강한 읽기. 레거시 자동 이관 |
| `netlify/functions/watch.js` | 개별 갱신, 특보변동 독립 발송, 알림 번호(eid) 반환, 문구 수정 |
| `netlify/functions/push.js` | 확인·reset·clear_ack을 개별 갱신으로 |
| `netlify/functions/_logbook.js` | `byType` 종류별 발송 집계 |
| `netlify/functions/watchdog.js` | **신규.** 5분마다 감시 생존 확인 + 자가복구 |
| `netlify/functions/diag.js` | `/api/diag`에 워치독 결과 노출 |
| `netlify.toml` | 워치독 스케줄, `/api/health` 경로 |
| `public/app.js` | 정체 표시 제외 (원래 동작 유지) |
| `public/sw.js` | 알림 번호 왕복. 배지 분리. 캐시 v39 → v41 |
| `public/icons/icon-192.png` | 물방울 → 영덕소방서 엠블럼 |
| `public/icons/icon-512.png` | 물방울 → 영덕소방서 엠블럼 |
| `public/icons/badge-96.png` | **신규.** 상태바 배지용 단색 실루엣 |
| `public/style.css` | 진입 애니메이션 부담 완화 (블렌드 모드·box-shadow 제거) |
| `public/manifest.webmanifest` | `short_name`을 "영덕소방"으로 변경 |
| `public/index.html` | 아이폰 홈화면 이름 통일 |
| `README.md` | 실제 구조에 맞게 갱신 |

## 고친 것

### 1. 창의 기준점이 시계가 아니었다
값이 들어있는 마지막 칸을 현재로 삼았다. 군청 표가 멈추면 몇 시간 전 비를
계속 현재로 판정했고, 비가 그쳐도 단계가 안 내려가 반복 알림이 멎지 않았다.
한국시각 기준 직전 완료 시각을 창의 끝으로 바꿨다.

### 2. 결측을 0으로 세고 있었다
`complete3` / `complete12` 플래그를 만들면서도 판정이 무시했다. 수치는 실측
합계만 담고, 결측 사실은 화면의 "일부결측"으로 그대로 알린다. 다만 군청이
무강우 시각을 빈칸으로 두는 경우와 구분하기 위해, 뒤에 값이 있는 앞쪽 빈칸은
0으로 확정한다.

### 3. 알림이 멈춰도 알 방법이 없었다
외부 스케줄러(cron-job.org) 1분 호출이 유일한 경로였다. Netlify 자체 스케줄러로
5분마다 생존을 확인하고, 멈춰 있으면 대신 돌린다.

### 4. 구독 명단이 통째로 덮어써지고 있었다
발송과 [확인]이 겹치면 확인 기록이 사라져 알림이 재개됐다.
`@netlify/blobs` 8.x에 조건부 쓰기가 없어 사람마다 키를 나눴다.
예전 배열은 첫 읽기 때 자동으로 쪼개져 옮겨지고 지워진다.

### 5. 특보 변동 알림이 반복 알림에 묻혔다
`else if` 사슬이라 반복이 도는 동안 상향·해제 알림이 막혔고, 다음 주기에는
변동으로 잡히지 않아 영영 오지 않았다. 독립 분기로 뺐다.

### 6. 반복 알림이 건별 기록에 안 남았다
`dispatch()`가 알림 번호를 돌려주지 않아 `logDispatch`가 첫 줄에서 빠져나갔다.
그 여파로 확인을 눌러도 건별 집계가 되지 않았다. 번호를 주기마다 하나 정해
모든 묶음에 넘기고 반환한다. `sw.js`도 그 번호를 확인 신호에 실어 보낸다.

### 7. 앱 아이콘을 영덕소방서 엠블럼으로
홈화면·알림 큰 아이콘을 물방울에서 엠블럼으로 바꿨다. 원형 마스크에 잘리지
않도록 안전영역(62%) 안에 넣었다.

상태바 배지는 따로 뒀다. 안드로이드가 알파 채널만 쓰고 흰색으로 칠하기 때문에
색이 든 아이콘을 넣으면 흰 사각형이 된다. 엠블럼은 선이 가늘어 24dp 단색
실루엣으로 만들면 알아볼 수 없어, 물방울 실루엣을 배지로 쓴다.
`logo.png`(헤더)는 원래도 같은 엠블럼이라 손대지 않았다.

### 8. 정시 직후 결측 표시 유예
군청은 17개 지점을 한꺼번에 채우지 않는다. 3시 정각에 15개가 들어오고
2~3분 뒤 나머지가 들어오는 식이다. 그 사이를 결측으로 띄우면 곧 채워질
것을 두고 걱정을 만든다.

방금 끝난 시각 한 칸만 비었고 정시 후 5분 안이면 표시하지 않는다.
앞 시각까지 비었거나 5분이 지났으면 그대로 알린다.
**표시만 미루고 계산은 그대로 한다** — 방금 끝난 시각을 창에서 빼버리면
그 시간에 온 폭우가 누적에서 사라진다.

5분은 실측값이 아니다. `data_lag_min` / `table_lag_min`을 새로 남기므로,
며칠 쌓아 군청이 정시 후 몇 분에 채우는지 확인하고 `_history.js`의
`GRACE_MIN`을 조정한다.

### 9. 진입 애니메이션 버벅임
두 가지가 원인이었다.

- `.cc-sheen`의 `mix-blend-mode: screen` — 블렌드가 걸리면 매 프레임
  아래 배경을 읽어와 섞느라 GPU 합성에서 빠진다. 카드 세 장이 동시에
  지나가는 진입 구간과 겹쳤다. 블렌드를 없애고 심 밝기로 보상했다.
- `.push-banner`의 `pbPulse` — `box-shadow`를 애니메이션하면 매 프레임
  다시 그려야 한다. 이 배너는 알림을 켜지 않은 동안 계속 떠 있어 부담이
  끊이지 않았다. 가상요소의 `transform` + `opacity`로 바꿨다.

`slideSettle`의 37단계 키프레임은 원인이 아니다. 브라우저는 인접한 두
구간만 보간하므로 개수와 무관하다.

남은 것: `edgeGlow`도 `box-shadow` 애니메이션이지만 단계가 올라간 카드에서
한 번만 돌아 두었다. 스파크라인 생성을 뒤로 미루는 작업(메인 스레드 경합)도
손대지 않았다.

### 10. 앱 이름 정리
짧은 이름을 "영덕강우" -> "영덕소방"으로 바꿨다.
홈화면 라벨, 알림 헤더, 아이폰 홈화면 세 곳에 쓰인다.

긴 이름(`name`)과 브라우저 탭(`<title>`)은 "기상위험 예방시스템"으로 둔다.
설치 안내창과 스플래시에 쓰이는 값이라 자리가 넉넉하다.
짧은 이름을 여기에 맞추면 홈화면 라벨과 알림 헤더에서 잘려
어느 앱인지 알아보기 어려워진다.

이미 설치한 사용자는 이름과 아이콘이 바뀌지 않는다. 안드로이드가 설치
시점 값을 잡아두기 때문이다. 재설치가 필요하다.

### 11. 알림 문구
강풍처럼 강우와 무관한 특보에서 "관내 강우 없음"을 뺐다.
호우·태풍 특보인데 관측 강우가 0인 경우에는 남긴다.

## 배포 전 확인

- `GITHUB_TOKEN`, `WATCH_TOKEN` 환경변수 필요. 워치독이 `WATCH_TOKEN`으로 watch를 부른다.
- 없어도 자가복구만 안 되고 나머지는 정상 동작한다.

## 배포 후 확인

- `/api/diag` → `health`에 값이 뜨는지 (최대 5분)
- `/api/watch?token=...` → `table_lag_h`가 0인지
- `/api/logs?token=...` → `byType`과 `events`가 채워지는지
- `/api/watch?token=...` → `table_lag_min`이 대체로 0~5 안인지 (며칠 보고 GRACE_MIN 조정)
- 화면에 "일부결측"이 상시로 뜨지 않는지
- `clear_ack`을 다시 쓸 일이 생기는지 (안 생기면 4번이 잡힌 것)

## 손대지 않은 것

- `_warning.js`의 특보 등급표 (설계 의도대로 유지)
- 시간별 그래프의 결측 구간 표시 (현행 유지)
- `_logbook.js`의 일별 집계 통째 쓰기 (통계라 유실돼도 알림에 영향 없음)
- `public/logo.png` (헤더 로고 — 이미 같은 엠블럼)
